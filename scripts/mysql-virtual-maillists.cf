# the user name and password to log into the mysql server
hosts = 127.0.0.1
user = postfix
password = FIXME
dbname = mail

table = cyrup_maillists
select_field = aliased_to
where_field = alias
additional_conditions = and enabled = '1'
